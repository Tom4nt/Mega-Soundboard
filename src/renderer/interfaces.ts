import { InfoBalloon } from "./elements";

export interface ITooltipHolder {
    tooltip: null; // TODO: Make tooltip an element. (Replace null)
}

export interface IInfoBaloonHolder {
    infoBaloon: InfoBalloon;
}